<div class="nav_list">
    <a class="nav_link"><i class="fa-brands fa-adversal"></i> Test</a>
    {% for item in items %}
        <a href="{{ path(item.route) }}" class="nav_link">
            <i class='bx bx-grid-alt nav_icon'></i>
            <span class="nav_name">{{ item.title }}</span>
        </a>
    {% endfor %}
</div>
